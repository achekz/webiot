name: iotapp
description: "Application de gestion intelligente de salles connectées."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.5
  image_picker: ^0.8.7
  firebase_core: ^2.12.0
  firebase_auth: ^4.6.0
  cloud_firestore: ^4.7.0
  fl_chart: ^0.62.0
  google_fonts: ^4.0.4
  flutter_svg: ^2.0.7
  intl: ^0.19.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    # Removed google_fonts/ directory as it does not exist
  generate: true