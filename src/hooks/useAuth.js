// Fichier pont pour garantir la compatibilité avec les importations existantes
// Ce fichier redirige simplement vers la version TypeScript

import { useAuth } from './useAuth.ts';

export { useAuth };
export default useAuth;
